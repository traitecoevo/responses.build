% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measurements.R
\name{get_instruments}
\alias{get_instruments}
\title{Load the instrument column maps a compilation uses}
\usage{
get_instruments(path = NULL)
}
\arguments{
\item{path}{Directory of instrument profiles. Defaults to
\code{config/instruments}, if it exists.}
}
\value{
A named list of profiles, keyed by both file stem and every value in
the profile's \code{matches}, so \verb{instrument: Li6400 IRGA} and
\code{instrument: licor_6400} both resolve. Empty if there is no directory.
}
\description{
Reads every \verb{config/instruments/*.yml}. Each maps the columns one instrument
writes onto variables, so a dataset can name the instrument instead of
restating twenty column mappings.
}
