% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/responses.R
\name{response_pivot_wider}
\alias{response_pivot_wider}
\title{Pivot a database's measurements to one row per point}
\usage{
response_pivot_wider(
  database,
  data_type = NULL,
  vars = NULL,
  numeric = TRUE,
  drop_empty = TRUE,
  with_curves = TRUE
)
}
\arguments{
\item{database}{A built database}

\item{data_type}{Keep only responses of these kinds, e.g. \code{"ACi"} or
\code{c("ACi", "ACi-T")}. Defaults to all of them.}

\item{vars}{Variables to include. Defaults to all of them.}

\item{numeric}{Coerce variables the definitions call numeric to \code{numeric}.
\code{TRUE} by default: values are stored as text, and every caller converts
them.}

\item{drop_empty}{Drop variable columns that are entirely \code{NA} after
filtering. \code{TRUE} by default, since a compilation-wide pivot of a dataset
that measured nine variables is otherwise mostly empty columns.}

\item{with_curves}{Attach the response's \code{data_type}, \code{driver} and
\code{taxon_name} to each row. \code{TRUE} by default, since a point is rarely
useful without knowing what it belongs to.}
}
\value{
A tibble, one row per point
}
\description{
Returns the wide view: one row per point of a curve, one column per variable.
This is the shape readings were taken in and the shape they are analysed in.
}
\section{Why this is a function and not a table}{
A wide table can hold a reading's value and nothing else. Measured on
AusFizz, 17 \verb{(dataset, variable, method_id)} groups carry more than one
\code{value_type} \emph{and} more than one \code{replicates} -- some variables recorded as a
mean of five, others raw -- and a single cell per \verb{(point, variable)} has
nowhere to put that. Storing both forms would either lose it or duplicate
147 Mb of readings to keep it.

So \code{measurements} is the lossless form and this is a view of it. It is one
\code{pivot_wider()} with nothing to re-derive, because \code{response_id} and \code{point_id}
are already columns -- which was the thing actually missing before responses
were first-class.

Where a variable has two values at one point, the pivot keeps the one from
the lowest \code{method_id}; \code{\link[=check_curve_points_conflicts]{check_curve_points_conflicts()}} reports what it
chose between.
}

\examples{
\dontrun{
# every A-Ci curve, wide, numeric, without the columns this study never used
response_pivot_wider(database, data_type = c("ACi", "ACi-T"))
}
}
