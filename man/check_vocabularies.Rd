% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measurements.R
\name{check_vocabularies}
\alias{check_vocabularies}
\title{Report descriptor values that are not in their controlled vocabulary}
\usage{
check_vocabularies(path = "data", vocabularies = get_vocabularies())
}
\arguments{
\item{path}{Directory of dataset folders, default \code{data}}

\item{vocabularies}{Descriptor vocabularies, from \code{\link[=get_vocabularies]{get_vocabularies()}}}
}
\value{
A tibble of dataset, descriptor and off-vocabulary value
}
\description{
The vocabularies are documentation of what a value may be, drawn mostly from
the ESS-DIVE leaf gas exchange standard and the Prometheus protocols. They
are not enforced at build time: measured across AusFizz, 73 of 315 declared
values sit outside their vocabulary, and most of those are real questions
about the data rather than typos -- \code{plant_age} holds actual ages where
ESS-DIVE expects a life stage, and \verb{upper canopy} and \code{upper_canopy} both
appear for the same thing.
}
\details{
Failing a build on those would block work on a documentation question. This
reports them instead.
}
