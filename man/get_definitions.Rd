% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_definitions}
\alias{get_definitions}
\title{Load the vocabulary of variables a compilation measures}
\usage{
get_definitions(path = NULL, dir = "config")
}
\arguments{
\item{path}{Path to the variable definitions. By default resolved inside
\code{dir}: \code{variables.yml}, falling back to \code{traits.yml} if that does not exist.}

\item{dir}{Directory holding the compilation's config, consulted when \code{path}
is \code{NULL}. Defaults to \code{config}.}
}
\value{
A list of variable definitions
}
\description{
Reads the definitions of the quantities that appear in the \code{value} column --
the readings taken at each point of a response curve: \code{A}, \code{Ci}, \code{gsw},
\code{Tleaf}, \code{PSIstem} and the rest.
}
\section{\code{variables}, not \code{traits}}{
These are instrument readings and measured state, not traits. A trait is a
derived parameter with one value per entity -- \code{Amax}, \code{Vcmax25}, \code{P50} --
and those live in \code{config/traits.yml}, which is where curve fitting will
eventually write. Calling the readings "traits" is what the upstream data
model did, and it is why 590,031 rows in AusFizz encode 50,089 measurements.

A compilation that still keeps its vocabulary in \code{config/traits.yml} is read
from there, with a message. That fallback exists so a repository can move at
its own pace; it is not a second supported layout.
}

\examples{
{
\dontrun{
definitions <- get_definitions()
}
}
}
