% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curves.R
\name{check_curve_points_conflicts}
\alias{check_curve_points_conflicts}
\title{Report variables with more than one value at the same point of a curve}
\usage{
check_curve_points_conflicts(database)
}
\arguments{
\item{database}{A built database}
}
\value{
A tibble of conflicting variable-point combinations, with the value
and \code{method_id} each came from, and \code{used} marking the one that reached
\code{curve_points}. Empty if there are none.
}
\description{
\code{curve_points} is wide, so it has one cell per variable per point. Where the
source measured the same variable twice within one curve -- under different
\code{method_id}s -- only one value fits, and the lowest \code{method_id} wins.
}
\details{
This finds those, so the choice is auditable. The \code{traits} table is
unaffected and still holds every value.

It is not warned about at build time. For curve data a conflict is rare and
worth looking at; for trait-style data, where one trait routinely carries
several methods, it is the normal case and would fire on every build.
}
