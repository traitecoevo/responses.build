% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/responses.R
\name{check_curve_points_conflicts}
\alias{check_curve_points_conflicts}
\title{Report variables with more than one value at the same point of a curve}
\usage{
check_curve_points_conflicts(database)
}
\arguments{
\item{database}{A built database}
}
\value{
A tibble of conflicting variable-point combinations, with the value
and \code{method_id} each came from, and \code{used} marking the one a pivot keeps.
Empty if there are none.
}
\description{
A curve holds one value per variable per point. Where the source measured the
same variable twice within one curve -- under different \code{method_id}s -- a
wide view of it can only show one. This finds those, so anyone pivoting with
\code{\link[=response_pivot_wider]{response_pivot_wider()}} knows what the pivot had to choose between.
}
\details{
It is not warned about at build time. For curve data a conflict is rare and
worth looking at; for trait-style data, where one variable routinely carries
several methods, it is the normal case and would fire on every build.
}
